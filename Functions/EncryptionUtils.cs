namespace GameLauncher.Functions;

public class EncryptionUtils
{
    public static string EncryptCookie(string cookie)
    {
        return cookie;
    }

    public static string DecryptCookie(string cookie)
    {
        return cookie;
    }
}